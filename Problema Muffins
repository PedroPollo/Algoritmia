#include<iostream>

using namespace std;

int main(){
   int N,M, resultado = 0;
   cout<<"Digite la cantidad de muffins que hay?:"; cin>>N;
   cout<<"¿Cuantas personas hay?:"; cin>>M;
   resultado = (N/M)+ (N%M);
   cout<<"A Gil le tocan:"<<resultado<<endl;
   return 0;
}
